# Llama-Server U盘便携版 使用教程

本套工具为**即插即用、全自动硬件调优**的离线大模型运行环境。无需安装任何环境或配置 Python，插入任意 Windows 电脑即可运行。

---

## 快速入门（三步启动）

1. **插入 U 盘并打开**：双击运行 U 盘根目录下的 `run.bat`。
2. **选择并加载模型**：
   - 在控制台主菜单中输入 `1` 并回车。
   - 界面会自动扫描 `Models` 文件夹下的所有 `.gguf` 模型文件。
   - 输入模型对应的数字编号并回车，系统将自动把模型全量载入 GPU 运行。
3. **开始对话**：
   - 启动成功后，保持控制台窗口不要关闭。
   - 返回主菜单输入 `3` 启动 Cherry Studio 桌面客户端，即可直接开始与本地大模型对话。

---

## 菜单功能说明

| 选项 | 功能描述 | 使用场景 |
| :--- | :--- | :--- |
| **1. 扫描并运行本地模型** | 自动识别 `.gguf` 格式模型，并以 GPU 全卸载（算力全开）模式启动 API 服务 | 每次需要使用模型时首选此项 |
| **2. 切换上下文大小** | 调整模型记忆/文本处理长度（默认为 16K） | 显存不够时调小，处理极长文档时调大 |
| **3. 启动 Cherry Studio** | 打开图形化聊天界面 | 提供类似 ChatGPT 的可视化对话界面 |
| **4. 启动 Hermes Agent** | 启动代码与工具链智能体终端 | 进阶功能，用于自动化代码与工具调用 |
| **5. 彻底停止服务并安全退出**| 释放 GPU 显存并关闭所有后台引擎 | 使用完毕拔出 U 盘前务必执行此项 |

---

## 硬件与显存适配指南

本工具集成了**硬件自适应调度算法**，会自动根据当前电脑的显卡配置调整运行策略：

- **NVIDIA 独显电脑**：自动开启 CUDA 硬件加速与 Flash Attention，将 100% 的计算量推送给 GPU 显卡，实现极速响应（70+ tokens/s）。
- **核显 / 集显 / 纯 CPU 电脑**：若检测不到 NVIDIA 驱动，系统会自动回退至纯 CPU 模式运行，保证程序不报错崩溃。

### 显存与上下文（Context Size）推荐对照

如果运行大模型时出现崩溃或提示显存溢出（Out of Memory），请在主菜单选择 `2` 调整上下文大小：

- **4 GB ~ 6 GB 显存**：建议设置为 **2K (2048)** 或 **4K (4096)**。
- **8 GB ~ 12 GB 显存**（如 RTX 3060/4060）：建议设置为 **8K (8192)** 或 **16K (16384)**。
- **16 GB 及以上显存**：可自由设置为 **32K** 或 **64K** 处理超长长文本。

---

## 进阶调用（开发者 API）

服务启动后，本地会默认监听 `http://127.0.0.1:8080`，提供标准 OpenAI 格式 API：

- **API 地址 (Base URL)**: `http://127.0.0.1:8080/v1`
- **API Key**: 任意填写（如 `sk-no-key-required`）
- **模型名称**: 任意填写或留空

可直接接入任何支持 OpenAI API 协议的第三方客户端（如 Page Assist 浏览器插件、Dify、RAGFlow 等）。

---

## 常见问题与排查

1. **更换电脑后打不开或找不到显卡？**
   - 请确保目标电脑已安装正常版 NVIDIA 官方显卡驱动。
2. **拔出 U 盘前需要做什么？**
   - 请在控制台中选择 `5` 彻底停止服务后再拔出 U 盘，避免后台进程残留或文件损坏。
3. **如何添加新模型？**
   - 将任意 `.gguf` 格式的模型文件直接放入 U 盘内的 `Models` 文件夹中，重新运行脚本即可自动识别。
